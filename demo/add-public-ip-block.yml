---
- hosts: 127.0.0.1
  connection: local
  tasks:

# Add public IP block
  - include_vars: dimensiondata_vars.yml
  - dimensiondata_public_ip_block:
      region: "{{region}}"
      location: "{{location}}"
      network_domain: testing_network111
      action: "{{action_add}}"
